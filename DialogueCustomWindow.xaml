<Window x:Class="PlanningScheduleApp.DialogueCustomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlanningScheduleApp"
        mc:Ignorable="d"
        Title="DialogueCustomWindow" Height="220" Width="300" ResizeMode="NoResize">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0 0 50">
            <TextBlock Text="{Binding Action}" FontSize="15" FontWeight="SemiBold" HorizontalAlignment="Center"/>
            <TextBlock Name="InfoAboutActionTBX" Text="{Binding Message}" FontSize="14"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0 0 0 10">
            <Button x:Name="YesBtn" Command="{Binding YesCommand}" Width="100" Height="35" Margin="5 0"/>
            <Button x:Name="CancelBtn" Command="{Binding NoCommand}" Width="100" Height="35" Margin="5 0"/>
        </StackPanel>

    </Grid>
</Window>
